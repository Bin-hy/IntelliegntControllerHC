On Linux platform, running terminal in this directory, and execute following lines:

$ sudo sh ./build.sh

 If all command execute successfully, find libUDEServer.so in build file, also runnable sample "ServerSample" will be created in this root directory path.