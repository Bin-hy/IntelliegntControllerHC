# duco机器臂和lhand机器手融合

## 需求
之前 duco机器臂和 lhand的机器手 是通过两个接口和工控机 进行通信的
之前是通过工控机和 duco机器臂的控制器（服务器）通过网线连接，控制器和duco机器臂进行连接。
工控机通过线（具体我不知道是usb线还是什么其他的）直连机器手。
都是通过ros2 下发指令，duco的控制器（服务器）接受指令控制duco机器臂，lhand的也是接受指令
现在我们希望将lhand的机器手的线连接到 duco的机器臂中，通过duco机器臂的工控机统一接受ros2指令。这样我只需要给duco机器臂的工控机下发指令，就可以控制duco机器臂和lhand的机器手。

## 现状
现在duco机器臂和lhand机器手他们已经将融合好了，说可以通过工控机统一进行控制了。请你帮我修改之前的代码逻辑使得我能够通过duco机器臂的工控机统一控制duco机器臂和lhand的机器手。


# 新需求
1. 任务功能中，对于相机功能需要将拍摄的耳机点云图数据位置并导出
2. 任务功能中，额外添加一个对于duco机器臂的IO进行控制，因为我们通过高低电平信号对点胶机，吹风机进行控制。因此任务中需要添加一个IO控制，利用这个函数控制控制柜上的IO输出口的高低电平：set_standard_digital_out(int16_t num, bool value, bool block) ; 先设置 num为 1的是 清洗机， 2的是吹风机。

## 实现控制清洗机平台升降RTU通讯说明：
协议类型：modbus-RTU
从设备ID 0x01 , 
串口号： COM5 , 波特率19200， 数据位：8， 停止位：1 ，校验码：EVEN
P001 (0x0001) 3 内部速度模式
P305 (0x0305) 100-3000prm 内部速度
p5267 (0x051b) 0010 寄存器数据16 //0000正转， 0010 反转



# orbbec相机碰撞检测功能
我需要通过orbbec连接的相机，实时检测任务中的机器臂是否发生碰撞。
在我的项目基础上提出一个方案，实现通过orbbec相机检测机器臂碰撞。
