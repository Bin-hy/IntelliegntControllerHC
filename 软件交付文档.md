# Intelligent Controller HC 软件交付文档

## 1. 项目简介
本项目（**IntelligentControllerHC**）是一个基于 ROS 2 (Jazzy) 和 Qt5 构建的智能协作机器人控制系统。该系统集成了多种前沿硬件设备（多可协作机械臂、因时灵巧手、奥比中光3D相机、数据手套等），旨在提供安全、高效、易用的智能机器人二次开发与任务执行平台。系统支持单/双臂构型，具备完善的用户权限管理、3D可视化、视觉识别及复杂任务编排能力。

## 2. 硬件环境
- **核心计算平台**：运行 Ubuntu 24.04 (或兼容 Linux 发行版) 的工控机或高性能 PC。
- **机械臂**：多可 (DUCO) 协作机械臂（支持全系列：GCR3, GCR5, GCR7, GCR10, GCR14, GCR16, GCR20, GCR25, GCR30 等单臂及双臂系统）。
- **末端执行器**：因时机器人 (Inspire-Robots) LHand Pro 系列灵巧手（如 DH116 等）。
- **视觉传感器**：奥比中光 (Orbbec) Gemini 330 3D 相机（支持单/双相机模式）。
- **其他外设**：数据手套 (Data Glove，用于遥操作或示教等)。

## 3. 软件环境
- **操作系统**：Ubuntu 24.04 (Noble Numbat)
- **核心中间件**：ROS 2 Jazzy Jalisco
- **GUI 框架**：Qt 5 (含 Qt3D、OpenGL 模块)
- **编程语言**：C++ (主) / Python (辅助脚本与 Demo)
- **运动规划**：MoveIt 2
- **依赖管理**：rosdep

## 4. 系统架构及核心模块
本项目采用 ROS 2 节点化分布式架构，主要包含以下核心包：
1. **用户交互层 (UI App)**: `ui_app`
   - 基于 Qt5 打造的综合操作软件。
   - 提供用户登录认证、权限管理、无操作自动登出。
   - 任务编排与记录管理模块。
   - 实时 3D 渲染组件：基于 URDF 的机器人姿态同步可视化 (`robot_viz_widget`) 与 3D 点云显示 (`point_cloud_widget`)。
2. **系统控制与逻辑层 (Controller & Logic)**:
   - `system_controller`: 系统任务调度节点，通过 Action Server 接受 UI 下发的任务序列并统筹各硬件节点执行。
   - `robot_control`: 机械臂运动学控制。
   - `safe_duco_status`: 独立的安全监控节点，实时检测机械臂状态确保作业安全。
   - `logger_node`: 统一系统日志记录。
3. **感知与视觉层 (Vision System)**:
   - `OrbbecSDK_ROS2` & `vision_server`: 提供双目/单目 3D 相机数据接入、点云处理和目标识别定位服务。
4. **硬件驱动层 (Drivers)**:
   - `duco_ros_driver`: 官方多可机械臂 ROS 2 底层通信驱动。
   - `lhandpro_service` & `lhandpro_interfaces`: 因时灵巧手驱动及控制接口，支持 RS485、CAN 等协议。
   - `glove_node`: 数据手套节点协议解析。
5. **模型与配置层 (Descriptions & Configs)**:
   - `duco_support`, `lhandpro_description`, `DH116_*`: 机器人和灵巧手 URDF/Xacro 模型库。
   - `*_moveit_config`: 涵盖从 GCR3 到 GCR30 乃至双臂 (`double_arm`) 的 MoveIt 规划配置文件。

## 5. 安装与部署
项目内提供了全自动的安装化脚本，可快速完成环境搭建：

**方式一：一键安装**
```bash
cd ~/IntelliegntControllerHC
sudo ./install.bash
```

**方式二：分步安装**
```bash
cd ~/IntelliegntControllerHC
# 1. 安装 ROS 2 Jazzy 环境
sudo ./install_ros2.bash
# 2. 配置并使用 rosdep 管理依赖
sudo ./install_rosdep.sh
# 3. 安装配置 Orbbec 相机驱动环境
sudo ./install_orbbec_env.bash
# 4. 安装灵巧手相关依赖
sudo ./install_lhand.bash
# 5. 安装 UI 应用级依赖 (Qt5 3D 及 OpenGL 等)
sudo ./install_ui_app.bash
```

**编译项目**
```bash
cd ~/IntelliegntControllerHC/ros2_ws
colcon build --symlink-install
```

## 6. 启动与运行
项目提供了一个集成式启动文件 `unified.launch.py`，一键拉起包括驱动、视觉、控制及 UI 在内的所有核心节点。

**执行命令：**
```bash
cd ~/IntelliegntControllerHC/ros2_ws
source install/setup.bash

# 启动全系统（请根据实际情况替换机械臂 IP 等参数）
ros2 launch ui_app unified.launch.py \
    robot_ip:=192.168.1.10 \
    model:=gcr5_910 \
    protocol:=tool_rs485
```

**可选参数说明：**
- `robot_ip`：机械臂控制器 IP 地址（默认 `192.168.1.10`）。
- `model`：机械臂型号（如 `gcr5_910`, `gcr10_1300`，默认 `gcr5_910`）。
- `protocol`：灵巧手通信协议，支持 `tool_rs485` (默认), `rs485`, `can`。
- `camera1_serial` / `camera2_serial`：指定奥比中光相机序列号（为空则自动匹配检测到的设备）。
